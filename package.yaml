name:                imageCompressor
version:             0.1.0.0
github:              "alexandre-BDT/imageCompressor"
license:             BSD3
author:              "Alexandre BDT"
copyright:           "2019 BDT"

dependencies:
- base >= 4.7 && < 5
- random >= 1.1

library:
  source-dirs: src

executables:
  imageCompressor:
    main:                Main.hs
    source-dirs:         app
    ghc-options:
    - -threaded
    - -rtsopts
    - -with-rtsopts=-N
    dependencies:
    - imageCompressor